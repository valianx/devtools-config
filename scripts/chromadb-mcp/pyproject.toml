[project]
name = "chromadb-mcp"
version = "1.0.0"
description = "Knowledge Graph MCP server backed by ChromaDB for cross-project memory"
requires-python = ">=3.10,<3.14"
dependencies = [
    "chromadb>=1.0.0",
    "mcp>=1.0.0",
    "uvicorn>=0.20.0",
]
